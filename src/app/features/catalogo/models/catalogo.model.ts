export interface PaisDto{
    pais: string;
    codigo: string;
}

export interface EmisorVehicularDto{
    codigo: string;
    nombre: string;
    abreviatura: string;
}

export interface TipoEstablecimientoDTO{
    tipo: string;
    codigo: string;
}