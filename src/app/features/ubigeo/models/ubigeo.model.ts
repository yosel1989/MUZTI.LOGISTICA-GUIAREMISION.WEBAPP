export interface UbigeoDto {
    id: string;
    departamento: string;
    provincia: string;
    distrito: string;
}

export interface UbigeoDepartamentoDto {
    id: string;
    departamento: string;
}

export interface UbigeoProvinciaDto {
    id: string;
    provincia: string;
}

export interface UbigeoDistritoDto {
    id: string;
    distrito: string;
}
